# React-appointment-webapp

```markdown

This is a simple appointment app built using react library.It allows user to add an appointment, look 
for an existing appointment, search appointments by applying filter as well such as sort by data and 
time of entry and also ascending and descending order.

The project is built using Visual Studio code using npm, node server and various react libraries.

```





